## Packages
framer-motion | Essential for the playful, bouncy animations required for a game
canvas-confetti | For the success celebration effect
@types/canvas-confetti | Types for the confetti library
lucide-react | Icon set (already in stack, but noting usage)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
